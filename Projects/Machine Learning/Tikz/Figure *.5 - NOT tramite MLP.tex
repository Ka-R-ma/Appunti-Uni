\documentclass{subfiles}
\begin{document}
\begin{figure*}[h!]
    \centering
    \begin{tikzpicture}[
            neuron/.style={
                    circle,
                    thick,
                    draw=black!80,
                    fill=gray!20
                }
        ]

        \node[neuron] (A) at (0, 0) {\(A\)};
        \node[neuron] (sum) [right = of A] {\(\Sigma\)};
        \node[neuron] (bias) [below = of sum] {\(b\)};
        \node[neuron] (out) [right = of sum] {\(f(x)\)};

        \path[-{Latex}]
        (A) edge (sum)
        (bias) edge (sum)
        (sum) edge (out);
    \end{tikzpicture}
\end{figure*}
\end{document}